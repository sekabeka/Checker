from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup

from src.bot.variables import MAX_URLS

def choose_url_to_remove_keyboard(urls: list):
    inline_keyboard = []
    for idx in range(MAX_URLS):
        pass
